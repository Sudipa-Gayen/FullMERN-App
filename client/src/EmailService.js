// EmailService.js
export const sendWelcomeEmail = (email) => {
    // Simulate sending welcome email (replace this with your actual email sending logic)
    console.log(`Welcome email sent to: ${email}`);
  };
  